#include <bits/stdc++.h>
using namespace std;
#define ln '\n'

int main(){
  long long x,y;
  cin>>x>>y;
  cout<<x<<" + "<<y<<" = "<<x+y<<ln;
  cout<<x<<" * "<<y<<" = "<<x*y<<ln;
  cout<<x<<" - "<<y<<" = "<<x-y<<ln;
  return 0;
}